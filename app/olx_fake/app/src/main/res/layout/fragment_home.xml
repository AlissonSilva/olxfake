<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rcListaAnuncios"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:itemCount="10"
        tools:layout_editor_absoluteX="0dp"
        tools:layout_editor_absoluteY="108dp"
        tools:listitem="@layout/lista_anuncio" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/fab"
        android:layout_width="0dp"
        android:layout_height="58dp"
        android:layout_gravity="start|bottom|end"
        android:layout_marginStart="90dp"
        android:layout_marginEnd="90dp"
        android:layout_marginBottom="20dp"
        android:gravity="center_vertical|start"
        android:text="Anunciar agora"
        android:textAlignment="center"
        android:textAllCaps="false"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Button"
        android:textColor="#FFF"
        android:textSize="15dp"
        app:cornerRadius="30dp"
        app:icon="@drawable/ic_add_a_photo_black_24dp"
        app:iconGravity="textStart"
        app:layout_constraintBottom_toBottomOf="@+id/rcListaAnuncios"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>